# Лекция 11. Move-семантика (базовая)

> Конспект к лекции для самостоятельного чтения. Слайды (`slides.md`) повторяют структуру, конспект подробнее объясняет «почему».

## 0. О чём эта лекция

Move-семантика — одна из главных фич C++11 и одна из тех вещей, которые отличают «современный» C++ от старого. Без неё было невозможно эффективно возвращать большие объекты из функций, хранить move-only типы вроде `std::unique_ptr` в контейнерах, делать `O(1)`-swap. С её появлением весь язык сильно перестроился — стандартная библиотека, идиоматический стиль, правила автогенерации функций.

Сегодня **базовая** часть истории:

- Зачем move-семантика существует — на примере с `swap` и возвратом больших объектов.
- Что делает `std::move` и почему это просто cast.
- Категории выражений (lvalue / rvalue) на уровне «достаточно для понимания move».
- Как написать свой move-конструктор и move-`operator=`.
- Правило пяти и правило нуля.
- Где move появляется в STL (`emplace_back`, `vector::reserve`).
- Когда `std::move` **не** нужно писать (RVO).

**Что мы не разбираем сегодня** (в элективную часть):

- **Universal references** (`T&&` в шаблонах) и **reference collapsing** — это про шаблоны, не про move как таковое.
- **`std::forward`** и **perfect forwarding** — следствие universal references.
- **Reference qualifiers методов** (`void method() &&`, `void method() &`).
- Полная классификация value categories (`xvalue`, `prvalue`, `glvalue`).

Базы достаточно, чтобы писать корректный современный C++. Перечисленное выше делает код более выразительным, но в этой части курса оно не критично.

## 1. Проблема: лишнее копирование

Классическая реализация `swap`:

```cpp
template <typename T>
void swap(T& a, T& b) {
    T tmp = a;
    a = b;
    b = tmp;
}
```

Для встроенных типов это нормально. Для `std::vector<int>` из миллиона элементов — **три копирования миллиона элементов**, каждое со своим malloc/memcpy. А ведь мы просто меняем местами — никаких «новых» данных никому не нужно, мы могли бы просто переставить **три указателя** внутри векторов.

Похожая история с возвратом из функций. Возьмём код, близкий к настоящему: агент вычитывает из системного буфера накопившиеся события и складывает пачки в очередь на обработку.

```cpp
std::vector<Event> read_batch() {
    std::vector<Event> batch;
    while (auto e = kernel_buffer.pop()) batch.push_back(*e);
    return batch;
}

std::vector<std::vector<Event>> pending;
pending.push_back(read_batch());
```

Что происходит без move-семантики:

1. Внутри `read_batch` собирается вектор `batch` — это первый объект.
2. На `return` он **копируется** во временный вектор — второй объект.
3. Временный **копируется** в `pending` — третий объект.
4. Первые два разрушаются.

Копирование вектора — это malloc плюс копирование каждого элемента. Если в пачке десять тысяч событий, мы сделали двадцать тысяч копирований объектов, из которых нужны ноль: и `batch`, и временный вектор гарантированно умрут через несколько инструкций. Хочется не копировать, а «переложить» содержимое — забрать у умирающего объекта его буфер вместе с правом собственности на него.

Заметим сразу, чтобы не создать ложного впечатления: конкретно этот пример современный компилятор частично спасёт и без нашего участия — механизмами copy elision и RVO, о которых поговорим в конце лекции. Но спасёт он именно частный случай «возвращаем локальную переменную». Общая проблема — «объект сейчас умрёт, а мы его копируем» — гораздо шире, и решается она move-семантикой.

Move-семантика — это именно механизм «переложить» вместо «скопировать».

## 2. `std::move` как инструмент

C++11 добавил способ сообщить компилятору: «вот этот объект мне больше не нужен, можешь забрать его потроха». Это и есть `std::move`.

С `std::move` `swap` становится:

```cpp
template <typename T>
void swap(T& a, T& b) {
    T tmp = std::move(a);
    a = std::move(b);
    b = std::move(tmp);
}
```

Для `std::vector` это переставит три указателя — O(1) — вместо копирования миллиона элементов. Для `std::string` — то же самое. Для `int` ничего не изменится (мува числа всё равно копирует, потому что у int нет ресурса).

### Что `std::move` делает на самом деле

`std::move` **не двигает ничего**. Он просто **меняет категорию выражения** — превращает lvalue в rvalue. После этого компилятор может выбрать move-перегрузку конструктора или оператора присваивания вместо copy-перегрузки.

```cpp
std::string s = "hello";
std::string t = std::move(s);   // здесь вызовется move ctor
```

Что произошло:

1. `std::move(s)` — это выражение с категорией rvalue, но содержащее тот же объект `s`.
2. Компилятор смотрит: «нам нужно создать `t` из rvalue типа `std::string`», и выбирает **move constructor** `std::string(std::string&&)` (а не copy).
3. Move-конструктор забирает внутренний указатель `s` и кладёт в `t`. У `s` указатель зануляется.

После операции `s` остаётся **валидным**, но в **неопределённом состоянии** (обычно пустая строка). Это важное обещание: после move с объектом всё ещё можно работать (он не «битый»), но содержимое его непредсказуемо.

```cpp
std::string t = std::move(s);
std::cout << s.size();   // ok, легально (обычно 0, но стандарт не гарантирует)
s = "new value";         // ok, переприсваивание работает
s.clear();               // ok, любая операция, не зависящая от содержимого
```

Использовать содержимое объекта после move до переприсваивания — **плохая идея**. Формально это не нарушение стандарта, но результат зависит от реализации, и такой код почти всегда оказывается ошибкой.

## 3. Value categories (lvalue / rvalue)

В C++ каждое выражение имеет **категорию**. Базовое деление — на две категории:

- **lvalue** (от «left value») — то, у чего есть **имя** и **адрес**. Может стоять слева от `=`. Переменная — lvalue.
- **rvalue** (от «right value») — временное значение. Литерал, результат функции, результат арифметики. То, что **не может** стоять слева от `=`.

```cpp
int x = 5;             // x — lvalue, 5 — rvalue
int y = x + 1;         // y — lvalue, x + 1 — rvalue
foo();                 // вызов функции — rvalue (если не возвращает T&)
x = 10;                // x — lvalue (можно присвоить)
10 = x;                // CE: 10 — rvalue (нельзя присвоить)
```

Интуиция: **rvalue** — это «что-то временное, что сейчас, возможно, умрёт». Если умрёт — можно безопасно «забрать» его внутренности, ему уже всё равно. **Lvalue** — это объект с именем; он переживёт текущее выражение, и забирать у него содержимое нельзя без явного разрешения.

`std::move(x)` — это именно «явное разрешение». Оно превращает lvalue `x` в rvalue.

### Правила привязки ссылок

Это правило важно для понимания move:

```cpp
int x = 5;
int& r = x;              // ok: lvalue-ссылка на lvalue
int& r2 = 5;             // CE: lvalue-ссылка не привязывается к rvalue
int&& r3 = 5;            // ok: rvalue-ссылка к rvalue
int&& r4 = x;            // CE: rvalue-ссылка не привязывается к lvalue
int&& r5 = std::move(x); // ok: std::move превращает x в rvalue
```

И ещё одно важное:

```cpp
const int& cr = 5;       // ok: const lvalue-ссылка может привязаться к rvalue
```

`const T&` исторически — «вечная» ссылка, которая принимает что угодно. Поэтому функции с `const T&` параметром принимают и lvalue, и rvalue без проблем.

Перегрузка `T(const T&)` (copy) vs `T(T&&)` (move) и определяет, **какой конструктор** будет вызван. Лекция дальше — про это.

### Полная классификация — обзор

Помимо lvalue и rvalue в C++ ещё есть **xvalue**, **prvalue**, **glvalue** — пять категорий. Они уточняют, что rvalue делятся на два подтипа. Для базы хватит «lvalue/rvalue»; полную классификацию разберём в элективной части. Если коротко: `std::move(x)` даёт **xvalue**, а литерал `5` — **prvalue**. Оба считаются rvalue для целей перегрузки.

## 4. Move-конструктор

Move-конструктор — это конструктор от **rvalue-ссылки** на свой тип:

```cpp
class String {
    char* data_ = nullptr;
    size_t size_ = 0;
public:
    String(const String& other);         // copy ctor

    String(String&& other) noexcept      // move ctor
        : data_(other.data_), size_(other.size_) {
        other.data_ = nullptr;
        other.size_ = 0;
    }
};
```

Внутри move-конструктора два важных действия:

1. **Забираем ресурс у источника** — копируем сам указатель, не его содержимое.
2. **Занудяем источник** — обнуляем его указатель и размер, чтобы его деструктор **не удалил нашу память**.

Это и есть «переложили ресурс».

### Когда вызывается move

```cpp
String a("hello");
String b = a;             // copy ctor: a — lvalue
String c = std::move(a);  // move ctor: std::move делает a rvalue
String d = String("hi");  // move ctor: String("hi") — rvalue (prvalue)
```

Компилятор смотрит на категорию выражения справа от `=`:

- Если **lvalue** — вызывается copy ctor.
- Если **rvalue** — вызывается move ctor (если есть; иначе копируется).

### `noexcept` обязателен (почти)

Move-конструктор почти всегда должен быть помечен `noexcept`:

```cpp
String(String&& other) noexcept { /* ... */ }
```

Причина в том, что `std::vector` (и многие другие STL-контейнеры) при перевыделении буфера **выбирают между move и copy** в зависимости от того, помечен ли ваш move-конструктор `noexcept`:

- **Помечен** — вектор использует move (быстро).
- **Не помечен** — вектор использует copy (медленно), потому что не может гарантировать **strong exception safety**: если посередине move бросилось исключение, исходные элементы уже разрушены, и откатиться некуда. С copy откатиться можно — просто выкинуть новые копии.

Это та же история, что мы разберём на лекции 14 про exception safety. Поэтому **всегда** помечайте move-функции `noexcept`, если они реально не бросают.

Если move-функция **может** бросать (например, использует throwing allocator) — `noexcept` не пишите. Иначе при броске будет `std::terminate`.

## 5. Move-`operator=`

Аналогично move-конструктору, но это присваивание уже существующему объекту:

```cpp
class String {
public:
    String& operator=(String&& other) noexcept {
        if (this == &other) return *this;

        delete[] data_;              // освобождаем своё
        data_ = other.data_;          // забираем чужое
        size_ = other.size_;
        other.data_ = nullptr;
        other.size_ = 0;

        return *this;
    }
};
```

Структура та же, плюс **освобождение нашего ресурса** до того, как заменим его на чужой. И **self-assignment check** — как в обычном `operator=`.

### Через copy-and-swap

Можно использовать ту же идиому, что для copy-`operator=`:

```cpp
String& operator=(String other) noexcept {
    swap(*this, other);
    return *this;
}
```

Это **одна функция** обслуживает и copy, и move:

- Если `b` это lvalue (`a = b`) — параметр `other` создаётся через copy ctor.
- Если `b` это rvalue (`a = std::move(b)` или `a = make_string()`) — через move ctor.

Внутри в любом случае swap, который для move-friendly типов почти бесплатный.

Цена — лишний swap для случая copy. В критичном по производительности коде пишут отдельные `operator=(const T&)` и `operator=(T&&)`. В обычном коде copy-and-swap проще и обычно достаточно.

## 6. Правило пяти и автогенерация

В C++ есть пять **special member functions**, которые компилятор может сгенерировать автоматически:

1. Default constructor — `T()`
2. Copy constructor — `T(const T&)`
3. Copy assignment — `T& operator=(const T&)`
4. Move constructor — `T(T&&)`
5. Move assignment — `T& operator=(T&&)`

Плюс деструктор (`~T()`), который тоже автогенерируется (поэтому иногда говорят «special member functions» и включают шестую).

### Правило пяти

Если вы написали **хотя бы одну из пяти** функций руками — нужно осознанно решить, что делать с **остальными**:

- Оставить автогенерированную (`= default`)
- Запретить (`= delete`)
- Написать руками

Самая частая ловушка — **деструктор**. Если вы написали свой деструктор, компилятор **не сгенерирует** move-функции автоматически (это правило C++11, чтобы не ломать совместимость со старым кодом). А copy-функции сгенерирует — и тип будет копироваться, хотя вы, возможно, хотели move.

```cpp
class Buffer {
    int* data_;
    size_t size_;
public:
    ~Buffer() { delete[] data_; }       // написали dtor

    // компилятор НЕ сгенерирует move ctor/op=! Класс будет копироваться по полям.
    // Если бы поля копировались (а не указатель!), это была бы катастрофа.
    // А указатель копируется, что приводит к double-delete.
};
```

Чтобы починить — `= default` move-функции:

```cpp
class Buffer {
public:
    ~Buffer() { delete[] data_; }
    Buffer(Buffer&&) noexcept = default;
    Buffer& operator=(Buffer&&) noexcept = default;
    // и copy-функции тоже пишем — руками или delete
};
```

### Правило нуля

Самое важное и самое часто применимое правило — **правило нуля**: если ваш класс **не владеет ресурсом напрямую**, не пишите вообще **ничего** из пятёрки.

Поля вроде `std::vector`, `std::string`, `std::unique_ptr`, `std::shared_ptr` сами умеют правильно копироваться и переноситься. Компилятор за вас сгенерирует все шесть функций правильно — они будут просто вызывать соответствующие функции для каждого поля.

```cpp
class Profile {
    std::string name_;
    std::vector<int> scores_;
    std::unique_ptr<Avatar> avatar_;
    // вообще ничего не пишем — всё работает корректно
};

Profile p1;
Profile p2 = p1;             // copy: каждое поле скопировано
Profile p3 = std::move(p1);  // move: каждое поле перенесено
```

Большинство хороших C++-классов следуют именно правилу нуля. Своя пятёрка нужна только в **низкоуровневых** классах, которые сами управляют ресурсами — `vector`, `string`, `unique_ptr` и тому подобные. В прикладном коде это редко.

## 7. `emplace_back` — конструирование на месте

### `push_back` vs `emplace_back`

```cpp
std::vector<std::string> v;
v.push_back("hello");      // создание временной string, потом move
v.emplace_back("hello");   // создание string прямо в векторе
```

`push_back(x)` принимает уже готовый объект (по copy или move). Если передан литерал, как `"hello"` — создаётся временный `std::string`, потом он перемещается в вектор. Это уже лучше, чем copy (благодаря move-семантике), но всё равно есть лишнее конструирование.

`emplace_back(args...)` принимает **аргументы конструктора** элемента и конструирует его **на месте**, прямо в памяти вектора. Без временного объекта. С variadic templates (лекция 9) это просто:

```cpp
template <typename... Args>
void emplace_back(Args&&... args) {
    // конструируем T прямо в позиции через placement new
    new (next_slot) T(args...);
    // ... (плюс правильное forward, см. элективную часть)
}
```

Аналогично есть `emplace`, `emplace_front`, `emplace_hint` у других контейнеров.

В новом коде предпочитайте `emplace_back`/`emplace` — обычно эффективнее и часто короче (`v.emplace_back(3, 4)` вместо `v.push_back(Point(3, 4))`).

### `vector::reserve`

Забегая вперёд, на лекции 14 будет подробный разбор exception safety. `vector::reserve` при перевыделении буфера должен **переложить элементы** из старого буфера в новый. До C++11 он делал это копированием — каждый элемент копируется в новое место.

С move-семантикой `vector` стал использовать **move**, если у элементов есть `noexcept`-move-конструктор. Иначе остаётся копирование (по причине strong exception safety, см. раздел про `noexcept` выше).

Это означает: правильно написанный move-конструктор вашего класса делает `vector<MyClass>` **в разы быстрее** при росте.

## 8. Когда **не** надо писать `std::move`

Очень частая ошибка — писать `std::move` там, где компилятор и так всё сделает хорошо.

### RVO — return value optimization

```cpp
std::string make() {
    std::string result = "hello";
    return result;           // НЕ std::move(result)!
}
```

Современный компилятор делает **NRVO** (named return value optimization): возвращаемый `result` **сразу строится** в памяти, выделенной вызывающей стороной. Никаких копий, никаких move — объект просто создаётся «там, где надо».

Если написать `return std::move(result)`, это **сломает RVO**: вместо «прямое конструирование на месте» получится «move в место». Move для `std::string` дешёвый, но всё равно лишний.

С C++17 для prvalue (например, `return std::string("hello")`) copy elision **обязательна** по стандарту — компилятор **должен** не делать копию/move. Для NRVO (как в примере выше) стандарт пока разрешает, но не обязывает; на практике все основные компиляторы это делают.

### Когда `std::move` нужен

- Когда **передаём что-то дальше**, и больше не будем использовать локально:
  ```cpp
  void set(std::string val);
  std::string local = "x";
  set(std::move(local));   // local нам больше не нужен
  ```
- Когда **извлекаем из контейнера** перед удалением:
  ```cpp
  auto x = std::move(v.back());
  v.pop_back();
  ```
- В реализации `swap`, как мы видели в начале.

### Когда `std::move` НЕ нужен

- `return local_var;` — RVO/NRVO сделает лучше.
- `return expression;` где `expression` уже rvalue (например, `return std::string("hi")`) — это уже rvalue.
- В контексте `noexcept`-move STL и так выберет move сама.

**Привычка:** не пишите `std::move` на `return` локальной переменной. Современные компиляторы выдают warning `-Wpessimizing-move`, когда `std::move` мешает RVO. Включайте этот флаг.

## Итоги

- **Move-семантика** позволяет **переместить ресурс** между объектами вместо копировать. Для `vector` — `O(1)` вместо `O(n)`.
- **`std::move(x)`** — это **cast**, который превращает lvalue в rvalue. Само ничего не двигает, даёт компилятору выбрать move-перегрузку.
- **lvalue** — то, у чего есть имя и адрес. **rvalue** — временные значения, которые могут «умереть сейчас».
- **Move-конструктор** `T(T&&)` забирает ресурс у источника и зануляет источник. **Move-`operator=`** — аналогично, плюс освобождает свой старый ресурс.
- **`noexcept`** на move-функциях критически важен: без него STL копирует вместо move.
- **Правило пяти:** написали одну из пятёрки → подумайте про остальные. **Правило нуля:** обычно вообще не пишите ничего из пятёрки, поля сами всё умеют. Большинство классов следуют правилу нуля.
- **`emplace_back`** конструирует на месте — экономит один move/copy относительно `push_back`.
- **На `return` локальной переменной `std::move` НЕ нужен** — компилятор сделает RVO лучше.

## Что дальше

Следующая лекция — **`auto` и `decltype`: кто пишет типы**:

- `auto` как делегирование: когда тип очевиден, писать его вручную — шум
- Чем `auto` за это платит: он *упрощает* тип, срезая `const` и ссылки
- `auto&`, `const auto&`, `auto&&` — как вернуть то, что срезалось
- `decltype` — способ узнать тип выражения, не называя его (`decltype(lambda)` уже встречался в лекции 10)
- `decltype(auto)` — синтез обоих механизмов для шаблонных обёрток

У темы move есть продолжение — **perfect forwarding**, universal references и полная классификация value categories, — но оно за рамками курса.
