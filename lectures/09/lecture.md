# Лекция 9. Variadic templates и правила вывода типов

> Конспект к лекции для самостоятельного чтения. Слайды (`slides.md`) повторяют структуру, конспект подробнее объясняет «почему».

## 0. О чём эта лекция

Это вторая лекция про шаблоны. На прошлой мы научились их **читать и писать** — обычные template-функции и классы, специализацию, перегрузку, dependent names. Сегодня — две вещи, которые делают шаблоны полноценным механизмом для современного C++:

- **Variadic templates** — шаблоны, принимающие **любое число** параметров. Без них не было бы `std::tuple`, `std::variant`, `std::make_unique`, `std::format`.
- **Fold expressions (C++17)** — выразительный способ операции над пакетом параметров. Заменяет рекурсию по pack'у на короткое выражение.

Заодно посмотрим на **правила вывода типов** — то, как именно компилятор определяет, что `T` это `int`, когда вы вызываете `add(2, 3)`. Это важно понимать, чтобы не путаться, когда ваш `T` оказался не тем, чего вы ждали.

К концу лекции вы должны уметь написать свой `make_unique`-подобный factory, понимать `std::tuple` и `std::format` на уровне сигнатур, и знать, почему `T&&` в шаблоне — не то же самое, что rvalue-ссылка. Зачем это различение нужно на практике, разбирается на лекции 11.

## 1. Variadic templates

Иногда функция должна принимать **любое количество** аргументов, возможно разного типа. Самый известный пример — `printf` в C:

```c
printf("Hello, %s, you are %d years old\n", name, age);
```

Это работает, но без типобезопасности — если форматная строка не совпадает с типами, в лучшем случае получаем мусор в выводе, в худшем — UB. C++ хочет тот же гибкий интерфейс, но с проверкой типов на компиляции. Для этого в C++11 ввели **variadic templates**.

На variadic templates построено очень многое в стандартной библиотеке: `std::tuple`, `std::variant`, `std::make_unique`/`make_shared`, `std::format`/`std::print`, `std::invoke`, всё семейство emplace-функций контейнеров. Это не «экзотическая» фича — это рабочий механизм современного C++.

### Синтаксис

```cpp
template <typename... Ts>
void print_all(Ts... args);
```

Несколько ключевых терминов:

- `typename... Ts` — **пакет шаблонных параметров** (template parameter pack). Это последовательность из нуля или более типов.
- `Ts... args` — **пакет аргументов функции** (function parameter pack). Соответствующие значения.
- `args...` — **раскрытие пакета** (pack expansion). Точки **после** того, что нужно «размножить».

Важно: пакет — это **не один тип/значение**, а **последовательность**. Из пакета нельзя «достать первый элемент» как из массива. Работать с ним можно только через раскрытие, рекурсию или fold expression.

### `sizeof...`

Узнать число элементов в пакете:

```cpp
template <typename... Ts>
void f(Ts... args) {
    std::print("Got {} args\n", sizeof...(args));
}

f();              // 0
f(1, 2.0);        // 2
f(1, 2.0, "x");   // 3
```

`sizeof...(args)` — отдельный оператор. Не путайте с `sizeof(args)`, у которого совсем другая семантика.

## 2. Раскрытие пакета

Пакет можно раскрыть в **разные контексты** — везде, где синтаксически уместен «список через запятую»:

```cpp
template <typename... Ts>
void forward_all(Ts... args) {
    other_func(args...);                    // в список аргументов
}

template <typename... Ts>
auto make_array(Ts... args) {
    return std::array{args...};             // в initializer list
}

template <typename... Bases>
class Multi : public Bases... {             // в список баз класса
    // ...
};
```

Раскрытие — это синтаксическое «применение тройной точки» к тому, что слева. `args...` означает «получи последовательность через запятую: `arg0, arg1, ..., argN`».

Точки `...` ставятся **после** «шаблона раскрытия» — того выражения или типа, которое нужно «применить к каждому элементу пакета». Например:

```cpp
template <typename... Ts>
void wrap_all(Ts... args) {
    other_func(std::move(args)...);    // эквивалент: std::move(arg0), std::move(arg1), ...
}
```

Здесь раскрывается `std::move(args)`, а не `args` — потому что точки стоят после `std::move(args)`.

## 3. Классическая рекурсия по пакету

До C++17 единственный способ обработать каждый элемент пакета по отдельности — рекурсия:

```cpp
void print_all() {}      // база рекурсии — пустой пакет

template <typename T, typename... Rest>
void print_all(T first, Rest... rest) {
    std::cout << first << ' ';
    print_all(rest...);  // рекурсивный вызов с остатком
}

print_all(1, 2.5, "hello");    // 1 2.5 hello
```

Идея: раскладываем пакет на «первый элемент + остаток». Обрабатываем первый, рекурсивно вызываем с остатком. Когда остаток пустой — срабатывает базовый случай.

### `if constexpr`: базовый случай без второй функции

Отдельная функция для пустого пакета нужна только затем, чтобы остановить рекурсию. Напрашивается проверить размер остатка прямо в теле:

```cpp
if (sizeof...(rest) > 0) {   // CE
    print_all(rest...);
}
```

Не компилируется. `sizeof...(rest)` известен на компиляции, но обычный `if` — конструкция времени выполнения: **обе** его ветки обязаны быть корректными. Для последнего элемента остаток пуст, компилятор всё равно должен скомпилировать вызов `print_all()` без аргументов — а такой перегрузки больше нет.

`if constexpr` (C++17) снимает именно это ограничение: условие вычисляется на компиляции, и **невыбранная ветка не инстанцируется**.

```cpp
template <typename T, typename... Rest>
void print_all(T first, Rest... rest) {
    std::cout << first << ' ';
    if constexpr (sizeof...(rest) > 0) {
        print_all(rest...);            // для пустого остатка ветки просто нет
    }
}

print_all(1, 2.5, "hello");            // 1 2.5 hello
```

Базовый случай исчез: в инстанциации `print_all<const char*>` рекурсивного вызова нет вообще.

**Правило:** `if constexpr` нужен там, где ветки предъявляют **разные требования к типу**. Вне шаблона он бесполезен — отброшенная ветка всё равно проверяется целиком, и обычный `if` с константным условием даёт ровно тот же результат.

Рекурсия в любом варианте инстанцирует по функции на каждый суффикс пакета, и на каждом шаге повторяется одно и то же. С C++17 для большинства таких задач рекурсия не нужна вовсе — есть fold expressions.

## 4. Fold expressions (C++17)

Fold expression — это синтаксис, чтобы **свернуть** пакет в одно значение через бинарный оператор. Аналог `reduce`/`fold` из функциональных языков.

### Базовый пример

```cpp
template <typename... Ts>
auto sum(Ts... args) {
    return (args + ...);
}

sum(1, 2, 3, 4);    // 10
```

`(args + ...)` — **унарная правая свёртка** по оператору `+`. Компилятор раскрывает её в:

```cpp
arg1 + (arg2 + (arg3 + arg4))
```

То есть применяет оператор справа налево по парам.

### Четыре формы

| Форма | Раскрытие |
|---|---|
| `(args op ...)` | `arg1 op (arg2 op (... op argN))` — унарная правая |
| `(... op args)` | `((arg1 op arg2) op ...) op argN` — унарная левая |
| `(args op ... op init)` | то же, что унарная правая, плюс `init` справа — бинарная правая |
| `(init op ... op args)` | то же, что унарная левая, плюс `init` слева — бинарная левая |

Разница между «правой» и «левой» — порядок применения. Для коммутативных операций (`+`, `*`, `&&`, `||`) — без разницы. Для некоммутативных (`-`, `/`, `,`) — важно.

Унарные формы требуют, чтобы пакет был **непустым**. Бинарные позволяют задать начальное значение для пустого случая.

### Практические примеры

```cpp
template <typename... Ts>
auto sum(Ts... args)         { return (args + ...); }

template <typename... Ts>
bool all_true(Ts... args)    { return (args && ...); }

template <typename... Ts>
bool any_true(Ts... args)    { return (args || ...); }
```

Особенно полезна **свёртка по запятой**:

```cpp
template <typename... Ts>
void print_all(Ts... args) {
    ((std::cout << args << ' '), ...);    // fold по ,
}

print_all(1, 2.5, "hello");    // 1 2.5 hello
```

`((expr), ...)` — стандартная идиома «выполнить выражение для каждого элемента пакета». Это часто заменяет рекурсию.

Бинарные формы с пустым пакетом:

```cpp
template <typename... Ts>
int sum(Ts... args) {
    return (0 + ... + args);    // sum() == 0, sum(1,2,3) == 6
}
```

С `init = 0` функция работает даже если пакет пустой.

## 5. Применения variadic в реальности

Variadic templates — не экзотика, а **рабочий инструмент** современной стандартной библиотеки.

### `std::make_unique` и `std::make_shared`

```cpp
template <typename T, typename... Args>
std::unique_ptr<T> make_unique(Args&&... args);
```

Принимает любые аргументы конструктора `T` и перенаправляет их. Так работают **factory-функции** в C++. Свой простой `make_unique` можно написать так:

```cpp
template <typename T, typename... Args>
std::unique_ptr<T> my_make_unique(Args... args) {
    return std::unique_ptr<T>(new T(args...));
}

auto p = my_make_unique<Point>(3, 4);   // Point(3, 4)
```

Один шаблон обслуживает любые типы и любое число аргументов. В настоящем `std::make_unique` вместо `Args...` используется `Args&&...` плюс `std::forward<Args>(args)...` — идеальная передача. Это нужно, чтобы корректно передавать rvalue и сохранять `const`; разбирается на лекции 11, когда появится move-семантика.

### `std::tuple`

```cpp
std::tuple<int, double, std::string> t{1, 2.5, "x"};
auto t2 = std::make_tuple(1, 2.5, "x");
```

`tuple` хранит **любое число** значений **разных типов**. Доступ — `std::get<0>(t)`, `std::get<1>(t)` и т.д. Сами объявления и реализация `tuple` — пример сложного variadic кода в стандарте.

### `std::format` и `std::print`

```cpp
std::print("{} {} {}\n", a, b, c);
```

Сигнатура:

```cpp
template <typename... Args>
void print(std::format_string<Args...> fmt, Args&&... args);
```

Тоже variadic. Тип каждого `{}` в форматной строке проверяется на этапе компиляции — отсюда type safety. Это и есть преимущество C++ versions над C-шным `printf`.

## 6. Правила вывода типов

В шаблонной функции компилятор **выводит** значение `T` из аргументов. У этого вывода есть несколько правил, и их полезно знать, чтобы не путаться, когда ваш `T` оказался не тем, чего вы ждали.

### Вывод для `T` (по значению)

```cpp
template <typename T>
void f(T x);

int i = 5;
const int& r = i;
f(i);     // T = int
f(r);     // T = int (а не const int&)
f(5);     // T = int
```

Когда параметр — `T` (по значению), компилятор отбрасывает `const`, `volatile` и **ссылки**. Логика: функция получает копию, для копии const/reference не имеют смысла. Это называется **decay**.

### Вывод для `T&` (по lvalue-ссылке)

```cpp
template <typename T>
void g(T& x);

int i = 5;
const int ci = 10;
g(i);     // T = int, x — int&
g(ci);    // T = const int, x — const int&
g(5);     // CE: rvalue не привязывается к T&
```

Когда параметр — `T&`, `const` сохраняется (становится частью `T`). Ссылка убирается из `T` (она уже есть в самой сигнатуре).

### Вывод для `T&&` (universal reference)

```cpp
template <typename T>
void h(T&& x);

int i = 5;
h(i);     // T = int& — потому что lvalue
h(5);     // T = int  — потому что rvalue
```

`T&&` в шаблонной функции — это **не** просто rvalue-ссылка. Это **universal reference** (термин Скотта Мейерса) или **forwarding reference**. Особенность:

- Если передан **lvalue** — `T` дедуцируется как `int&`, и `T&&` после **reference collapsing** становится `int&`.
- Если передан **rvalue** — `T` дедуцируется как `int`, и `T&&` остаётся `int&&`.

Это позволяет одной функции принимать и lvalue, и rvalue, и передавать дальше — идеальная передача через `std::forward`. Она разбирается на лекции 11: без move-семантики говорить о ней рано.

### Правило выживания

- Хотите, чтобы const сохранился? → `f(const T& x)` или `f(T& x)`.
- Хотите менять — `f(T& x)`.
- Хотите универсальную функцию, которая берёт что угодно — `f(T&& x)`, и внутри `std::forward<T>(x)` (лекция 11).
- Нужна копия? — `f(T x)`, но дорого для больших объектов.

## Итоги

- **Variadic templates:** `template <typename... Ts>`, пакет параметров, раскрытие `args...`, `sizeof...(args)` для размера.
- **Раскрытие пакета** работает в разных контекстах: список аргументов, initializer list, список баз класса.
- **`if constexpr` (C++17):** невыбранная ветка не инстанцируется. Убирает отдельную функцию для базового случая рекурсии; вне шаблона бесполезен.
- **Fold expressions (C++17):** `(args + ...)`, четыре формы. Свёртка по `,` для «выполни для каждого». Бинарные формы для пустых пакетов.
- **Применения:** `make_unique`, `tuple`, `format`, ваши factory-функции — везде на variadic templates.
- **Вывод типов в шаблонах:** для `T` отбрасываются const/ref; для `T&` const сохраняется; `T&&` — universal reference, и что с ней делать внутри — лекция 11.

## Что дальше

Следующая лекция — **лямбды и `std::function`**:

- Лямбды как **автогенерируемые шаблонные функторы** — это естественное продолжение после классов с `operator()` (лекция 5) и шаблонов (лекции 8–9)
- Списки захвата (by value, by reference, mixed)
- Захват `this` и полей класса
- Захват с инициализацией
- **`std::function`** — универсальный тип-контейнер для любого callable (функция, лямбда, функтор)
