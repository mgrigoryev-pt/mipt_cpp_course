# Занятие 1.3. Функции, каналы отказа и таблица правил

> [Лекция 3](../lectures/03/lecture.md): функции, перегрузка, ссылки, `const`, касты, указатели на функции,
> как функция сообщает об ошибке, `noexcept`.

Главное занятие первой части. Код из 1.1 и 1.2 разбирается на функции, у каждой
функции появляется явно выбранный канал отказа, и в проекте появляется настоящий
движок правил — самый примитивный из возможных.

## Конфигурация

```bash
cmake --preset windows -D NANO_EDR_LESSON=1.3
cmake --build --preset windows
ctest --preset windows
```

На Linux вместо `windows` — `linux`. Что делают эти команды и откуда их
запускать — в [`project/README.md`](../project/README.md).

**Набор тестов 1.1 с этого занятия не подключается, и это часть задания,
а не поломка.** Он сравнивал вывод с эталоном подстрочного поиска. Детект
на этом занятии — другая сущность: не подстрока в строке, а сработавшее
правило. Проверять новый контракт старым эталоном нечестно, поэтому эталоны
заменены. Набор 1.2 остаётся: разбор строки и список на этом занятии
не меняются, и если они сломались, то сломались при рефакторинге — узнать
об этом лучше сразу.

Какие наборы действуют на каком занятии, перечислено в `tasks/lessons.cmake`.
Набирать оттуда ничего не надо: номера занятия достаточно.

## Куда это писать

Два новых файла в `src/` плюс третий — на ваши правила:

| | |
|---|---|
| `src/fields.cpp` | реализации из `fields.h` — слой доступа к полям |
| `src/rules.cpp` | реализации из `rules.h` — механика таблицы |
| `src/agent_rules.cpp` + `.h` | сами правила и `constexpr`-таблица; имя ваше |

Реализовать нужно всё, что объявлено в заготовках, и ровно с теми подписями:

```
FindField  GetRequiredField  GetIntField (две перегрузки)
IsProcessStart  IsFileWrite  IsNetConnect
PathEndsWith  CommandLineContains  NormalizePath
SeverityName  CheckRules
```

`CMakeLists.txt` править не нужно: библиотека `nano_edr_core` собирается
из всех `.cpp` в `src/`, кроме `main.cpp`. Но появление новых файлов —
изменение описания проекта, поэтому после него нужна **пере**конфигурация.

**Пока этих файлов нет, набор 1.3 не собирается** — компоновщик скажет
`unresolved external symbol` (на Linux — `undefined reference`) на каждую
функцию из заготовок. Так выглядит начало занятия.

**Таблица правил должна лежать в библиотеке, а не в `main.cpp`.** Иначе
выданные тесты до неё не достанут: они линкуются с `nano_edr_core`,
а `main.cpp` в неё не входит.

## На паре

### Рефакторинг в функции

`main` из занятия 1.2 разбирается на части: разбор аргументов, цикл прогона,
печать сводки, подсчёт по типам. Рядом появляется слой доступа к полям —
`FindField`, `GetRequiredField`, `GetIntField` — и предикаты `IsProcessStart`,
`PathEndsWith`, `CommandLineContains`.

Параметры — по ссылке или по значению осознанно, а не как получилось.

### Каналы отказа

Для каждой функции слоя доступа надо явно решить, каким каналом она сообщает
об отказе. Их два, и выбор не стилистический:

| Отказ | Канал |
|---|---|
| поля нет в событии | `nullptr` — это один из двух ожидаемых исходов |
| строка журнала битая | `bool` / счётчик — битые строки бывают всегда |
| запрошено обязательное поле, которого нет | `throw std::invalid_argument` — нарушен контракт формата |
| запрошен индекс за границами | `throw std::out_of_range` |

Разница между первой и третьей строкой — не в механизме, а в том, кто виноват.
«Поля `cmdline` нет» — нормальное событие. «Нет поля `ts`» — журнал
не соответствует формату, и продолжать нельзя.

Плюс `catch (const std::exception& e)` в `main` как последний рубеж: программа
обязана печатать причину и завершаться штатно, а не умирать молча. Обработчик
пишется **до** первого `throw`: иначе первая же проверка на битом журнале даст
аварийное завершение без сообщения.

### Таблица правил на указателях на функции

```cpp
using RuleFn = bool (*)(const Event&);
struct Rule { const char* id; RuleFn check; Severity severity; };

constexpr Rule kRules[] = {
    {"script_host_from_temp", IsScriptHostFromTemp, Severity::kHigh},
    // ...
};
```

Агент прогоняет каждое событие по таблице и печатает детекты.

Это уже движок правил. На занятии 3.1 вы увидите, во что он превращается,
и вопрос «зачем было нужно то, что было раньше» будет иметь конкретный ответ.

## Дома

- Пять правил в таблице.
- `NormalizePath`: регистр, слеши, `%TEMP%`.
- Перегрузки `GetIntField`.
- Таблица тестов на каждую функцию слоя доступа.
- **Доказать на своём коде утечку, от которой избавил `unique_ptr`.**
  На занятии 1.2 список переехал на `unique_ptr` и течь перестал — но отчёта
  вы не видели, а значит и не знаете. Верните список на сырые указатели
  во временной копии файла, бросьте исключение из середины наполнения,
  соберите пресетом `asan-windows` (на Linux — `asan`) и прочитайте отчёт.
  Затем верните `unique_ptr` и убедитесь, что отчёт пуст. Сдаётся версия
  с `unique_ptr`; смысл упражнения в том, что красный прогон вы видели своими
  глазами.

## Готово, когда

- На `phishing_macro` детекты есть, на `clean_office` их нет.
- У каждой функции слоя доступа канал отказа задокументирован — одной строкой
  в комментарии, и строка объясняет выбор, а не повторяет подпись.
- Программа не падает молча ни на одном битом журнале. Коды возврата
  различают исходы: **0** — прогон дошёл до конца, **1** — прогон оборван
  нарушением контракта формата и причина напечатана, **2** — работать не с чем
  (аргументы, файл не открылся).
- Ревью не находит лишних копий строк. **С этого занятия за `std::string`
  по значению вместо `const std::string&` снимаются баллы** — ссылки пройдены.

## Продвинутая часть

Одно из двух.

**Режим `--explain`**: печатает, какое условие какого правила не выполнилось
на данном событии. Полезнее, чем кажется: без него отладка правила —
угадывание.

Задание больше, чем выглядит, и первый вопрос стоит задать себе до того,
как писать: как функция, у которой на выходе `bool`, назовёт причину?
Ответа «никак» она не примет, а подпись `RuleFn` менять нельзя — против неё
собраны выданные тесты.

**Сборка с `-fno-exceptions`**: собрать проект и перечислить, что пришлось бы
изменить в дизайне отказов. Симулятор с этим флагом собирается — значит препятствие
только в вашем коде, и интересно, какое именно.

## Что выдаётся

- `kit/include/l1.3/` — срез заготовок занятия. Нового два файла: `fields.h`
  (слой доступа к полям с каналами отказа) и `rules.h` (`Severity`, `Rule`,
  `RuleFn`, `CheckRules`). Остальные перенесены с `l1.2` без изменений.
- `tasks/1.3/tests/fields_tests.cpp` — тесты слоя доступа, включая обе формы
  отказа.
- `tasks/1.3/tests/rules_tests.cpp` — тесты механики таблицы.

Подробнее — `tasks/1.3/tests/README.md`.

## На что смотреть

**`FindField` возвращает указатель, а не ссылку.** Ссылку не на что вернуть,
если поля нет; ссылка на статический пустой объект — ложь вызывающему. Это
первый в курсе случай, где выбор типа возврата и есть выбор контракта.

**Старые подписи остаются на указателях.** `ParseEventLine`, `IsBlankOrComment`,
`ListPushBack` написаны до лекции 3, и переписывать их на ссылки не надо:
против ссылочных подписей не собираются выданные тесты занятия 1.2, а проверяют
они поведение, которое здесь не менялось. В итоге в одном цикле рядом стоят обе
формы — это нормальная картина живого кода, и вопрос «менять ли теперь»
решается ценой поломки вызывающих, а не вкусом.

**Указатель на функцию не имеет состояния.** Правило «двадцать событий за десять
секунд» этой таблицей не выражается никак. Подумайте, почему, — ответ появится
на занятии 3.1, и он не про синтаксис.
