# Занятие 2.2. Условия, базовый класс правила и механика наследования

> [Лекция 6](../lectures/06/lecture.md): наследование, layout объекта, порядок конструирования и разрушения,
> множественное наследование.

Честно: ромб и виртуальное наследование в nano-edr не нужны. Натягивать их
на проект было бы видно сразу, поэтому механика языка — отдельным коротким
упражнением, без легенды про EDR. А проект в этот день получает базовый класс
правила.

## Упражнение (30 минут)

`tasks/2.2/exercise/inheritance.cpp` — отдельная программа, собирается вручную.

Четыре части, и в каждой сначала предсказание, потом проверка:

1. Класс с двумя базами и тремя полями. **Предсказать на бумаге** порядок
   конструирования и разрушения, потом проверить трассировкой.
2. Посчитать `sizeof` и объяснить padding. Не «компилятор так решил», а какие
   именно байты и почему.
3. Посмотреть, **как меняется значение указателя** при приведении к второй базе.
   Это то самое, из-за чего на 2.1 трамплину надо передавать указатель
   на интерфейсную часть.
4. Собрать ромб, увидеть проблему, починить виртуальным наследованием
   и сравнить `sizeof` до и после.

Предсказание до запуска — не формальность. Замер без догадки ничему не учит:
он просто сообщает числа, и любые числа кажутся ожидаемыми.

## Проект (остаток пары и дом)

- **`RuleBase`** с общими полями: идентификатор, severity, список действий,
  счётчик срабатываний.
- **`MatchRule : RuleBase`**, хранящий вектор условий из занятия 2.1.

Метод `Check` пока **невиртуальный**. Виртуальность появится на следующем
занятии, и об этом сказано заранее не случайно: интересно, где вы упрётесь
до того, как её получите.

## Готово, когда

- Предсказанный порядок конструирования и разрушения совпал с фактическим.
- Вы объясняете, почему `MatchRule` нельзя хранить в `std::vector<RuleBase>`.

Второй пункт — не про синтаксис. Ответ на него определит устройство набора
правил на занятии 2.3.

## Продвинутая часть

Миксин `Counted` через множественное наследование вместо поля-счётчика.
Собрать оба варианта и сказать, что здесь лучше и почему.

Ответ не обязан быть в пользу миксина. Интересна аргументация, а не выбор.

## Что выдаётся

- `kit/include/l2.2/` — `RuleBase`, `MatchRule`, `Severity`.
- `tasks/2.2/tests/rule_tests.cpp` — тесты правила.
- `tasks/2.2/exercise/inheritance.cpp` — упражнение и `README.md` рядом.

## На что смотреть

**Упражнение и проект в этот день не связаны.** Так и задумано: ромба в проекте
нет, и делать вид, что есть, было бы хуже, чем признать это вслух.

**Счётчик срабатываний — состояние.** Значит `Check` не может быть `const`, и это
всплывёт на 2.3 при объявлении интерфейса. Честный неконстантный метод лучше,
чем `mutable`-поле под словом `const`.
