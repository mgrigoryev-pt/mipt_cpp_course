# Занятие 2.2. Условия, базовый класс правила и механика наследования

> [Лекция 6](../lectures/06/lecture.md): наследование, layout объекта, порядок конструирования и разрушения,
> множественное наследование.

Ромб и виртуальное наследование в проекте не нужны, поэтому механика языка
идёт отдельным коротким упражнением, без легенды про EDR. Проект в этот день
получает базовый класс правила, и с упражнением он не связан.

## Конфигурация

```bash
cmake --preset windows -D NANO_EDR_LESSON=2.2
cmake --build --preset windows
ctest --preset windows
```

На Linux вместо `windows` — `linux`. Что делают эти команды и откуда их
запускать — в [`project/README.md`](../project/README.md).

**Наборы тестов 2.1 и 2.2 действуют оба.** Событие, разбор, окно, обёртка
над границей и четыре условия-функтора не изменились, поэтому набор 2.1 обязан
остаться зелёным: перепись правил не имеет права задеть ничего, кроме правил.
Если что-то из старого набора покраснело — сломалось при переделке, и узнать
об этом лучше сразу.

Какие наборы действуют на каком занятии, перечислено в `tasks/lessons.cmake`.

## Куда это писать

Один новый файл в `src/`, три переписываются:

| | |
|---|---|
| `src/rule.cpp` | реализации из `rule.h` — `RuleBase` и `MatchRule` |
| `src/conditions.cpp` | к четырём функторам добавляется `Condition` |
| `src/agent_rules.cpp` | набор правил: объекты вместо таблицы функций |
| `src/rules.cpp` | из трёх сущностей осталась одна — `SeverityName` |

Плюс правится ваш агент: вектор правил вместо таблицы и сводка по счётчикам.

Реализовать нужно всё, что объявлено в заготовках, и ровно с теми подписями:

```
RuleBase::RuleBase  AddAction
MatchRule::MatchRule  AddCondition  Check
Condition::Equals  Contains  EndsWith  operator()
```

`id()`, `severity()`, `hits()`, `actions()`, `CountHit()`, `conditions()`,
`match()`, `key()`, `values()` написаны в заголовках — их реализовывать
не надо.

**Пока `rule.cpp` не появился, набор 2.2 не собирается**: компоновщик скажет
`unresolved external symbol` (на Linux — `undefined reference`). Так выглядит
начало занятия. И помните, что появление нового файла — изменение описания
проекта, поэтому после него нужна **пере**конфигурация.

## Упражнение (30 минут)

`tasks/2.2/exercise/inheritance.cpp` — отдельная программа, собирается вручную;
команды для обеих платформ — в `README.md` рядом с ней.

Четыре части, и в каждой сначала предсказание, потом проверка:

1. Класс с двумя базами и тремя полями. **Предсказать на бумаге** порядок
   конструирования и разрушения, потом проверить трассировкой.
2. Посчитать `sizeof` и объяснить padding. Не «компилятор так решил», а какие
   именно байты и почему.
3. Посмотреть, **как меняется значение указателя** при приведении к второй базе.
   Это то самое, из-за чего на 2.1 трамплину надо передавать указатель
   на интерфейсную часть.
4. Собрать ромб, увидеть проблему, починить виртуальным наследованием
   и сравнить `sizeof` до и после.

Предсказание до запуска — не формальность. Замер без догадки ничему не учит:
он просто сообщает числа, и любые числа кажутся ожидаемыми.

## Проект (остаток пары и дом)

- **`RuleBase`** с общими полями: идентификатор, severity, список действий,
  счётчик срабатываний.
- **`MatchRule : RuleBase`**, хранящий вектор условий.
- **`Condition`** — пятый класс условия, и с него стоит начать: без него
  вектора не будет.

Про `Condition` — потому что шаг неприятный, и его неприятность и есть
содержание занятия. Правило — это «все условия выполнены», значит условия
нужно **сложить в вектор**. Четыре функтора занятия 2.1 — четыре разных типа,
а вектор хранит объекты одного типа, и свести их к одному пока нечем. Отсюда
`Condition`: один класс, внутри переключатель вида сравнения. Решение
временное, и в заголовке это сказано прямо.

Заодно выяснится, что видов сравнения **три, а не четыре**: `FieldInList` —
это `FieldEquals` со списком значений, и ничего больше. Отдельным видом он
выглядел только потому, что был отдельным классом.

Метод `Check` в заготовке **невиртуальный**, и добавлять к нему ничего
не надо. Прямое следствие: правила приходится хранить вектором конкретного
типа — `std::vector<MatchRule>`, а не `std::vector<RuleBase>`. Почему именно
так — последний пункт «Готово, когда».

## Готово, когда

- Предсказанный порядок конструирования и разрушения совпал с фактическим.
- **Все семь эталонов совпадают с занятием 2.1 побайтово**, и оба выданных
  набора тестов зелёные. Правила переписаны целиком — с указателей на функции
  на объекты, — и это единственное доказательство, что перепись ничего
  не сломала: «вроде то же самое» доказательством не является. Сравнивать
  удобно под `--quiet`, где в `stdout` только детекты.
- Печатается сводка по счётчикам: сколько раз сработало каждое правило.
  Правило, не сработавшее ни разу на всём наборе сценариев, скорее всего
  написано неверно, и увидеть это в сводке дешевле, чем узнать на разборе.
- Вы объясняете, почему `MatchRule` нельзя хранить в `std::vector<RuleBase>`,
  **и показываете это на своём коде**: такой вектор компилируется, элемент
  в него кладётся, а `Check` у элемента вызвать нечем. Компилятор про это
  молчит — увидеть надо своими руками.

## Продвинутая часть

Счётчик срабатываний — это поле. Есть второй способ: **миксин** — маленький
класс, который умеет только считать и приклеивается вторым базовым классом
тому, кому счёт нужен.

```cpp
class CountedEvents {
 public:
    std::size_t events() const { return events_; }
 protected:
    void CountEvent() { ++events_; }
 private:
    std::size_t events_ = 0;
};
```

`RuleBase` и `MatchRule` — заготовки, и менять их нельзя: и поле, и метод,
и геттер лежат там. Поэтому приём применяется к тому, что ваше целиком, —
**к агенту**, где счётчиков несколько и все они поля.

Переведите на миксины **два** счётчика: события и детекты. Второй и есть
задание: одним классом их не посчитать, и запрещено это двумя разными
способами. Найдите оба и назовите — компилятор скажет прямо, надо только
попробовать.

**Что сдаётся, кроме кода:**

- `sizeof` агента до и после — с предсказанием **до** замера;
- что стало со значением указателя при приведении к типу второго миксина,
  и почему на первом этого не видно;
- сколько классов понадобилось, чтобы посчитать два числа, и сколько
  понадобилось бы на все счётчики агента;
- ответ: что здесь лучше — поле или миксин — и почему.

Ответ «поле» не поражение: приём может оказаться дороже задачи, и назвать
цену — половина работы. Вывод без цены не считается.

**Условие:** вывод на сценариях не меняется ни на байт, оба набора тестов
остаются зелёными. Счётчики считают то же, что считали.

## Что выдаётся

- `kit/include/l2.2/` — новое здесь `rule.h` (`RuleBase` и `MatchRule`)
  и `Condition` в `conditions.h`. `rules.h` наоборот сократился: таблица
  указателей на функции ушла, остались `Severity` и `SeverityName`.
  Остальные заголовки перенесены с `l2.1` без изменений.
- `tasks/2.2/tests/rule_tests.cpp` — тесты правила и условия.
- `tasks/2.2/exercise/inheritance.cpp` — упражнение и `README.md` рядом.

## На что смотреть

**Счётчик срабатываний — состояние.** Значит `Check` не может быть `const`:
честный неконстантный метод лучше, чем `mutable`-поле под словом `const`.

**Условие не бросает, и вместе с исключением кое-что теряется.** Правило-функция
занятия 2.1 звала `GetRequiredField` и объявляла отсутствие обязательного поля
нарушением контракта. Условие на том же событии просто не выполняется — и это
лучше, но отличить «поля нет» от «поле не подошло» больше нечем. Размен
сознательный; знать, что он сделан, полезнее, чем не заметить его.
